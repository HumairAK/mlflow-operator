apiVersion: mlflow.opendatahub.io/v1
kind: MLflow
metadata:
  name: mlflow
spec:
  # Minimal configuration for e2e testing
  # Disables kube-rbac-proxy for direct access
  kubeRbacProxy:
    enabled: false

  # MLflow container image
  image:
    image: quay.io/opendatahub/mlflow:latest
    imagePullPolicy: Always

  # Single replica for testing
  replicas: 1

  # Minimal resources
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"

  # Local file-based storage with PVC
  storage:
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 5Gi

  # SQLite backend for simplicity
  backendStoreUri: "sqlite:////mlflow/mlflow.db"
  registryStoreUri: "sqlite:////mlflow/mlflow.db"
  artifactsDestination: "file:///mlflow/artifacts"

  # Required for file-based artifact storage
  serveArtifacts: true

  # Enable debug logging for troubleshooting
  env:
    - name: MLFLOW_LOGGING_LEVEL
      value: DEBUG
